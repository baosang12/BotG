name: Manual diag
on:
  workflow_dispatch:
    inputs:
      note:
        description: 'why'
        required: false
        default: 'ping'
jobs:
  diag:
    timeout-minutes: 10
    runs-on: ubuntu-latest
    steps:
      - run: echo "manual-dispatch-ok"

