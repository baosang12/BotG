## Manual git commands for CI call-operator fix (timestamp: 20250828_000000)

# Use Git Bash at a real git clone (ASCII-safe path recommended):
#   C:\\w\\BotG

# If you need a clean clone:
mkdir -p /c/w
cd /c/w
git clone https://github.com/baosang12/BotG.git
cd /c/w/BotG
git checkout -b ci/recon-fix-20250828_000000

# Copy these updated files from your VS Code workspace into this clone and overwrite:
#   .github/workflows/ci.yml
#   scripts/ci_reconstruct_validate.ps1

# Commit and push:
git add .github/workflows/ci.yml scripts/ci_reconstruct_validate.ps1
git commit -m "CI: call-operator for recon validate + markers"
git push -u origin HEAD

# After push, a new Actions run should start. In job 'Reconstruct & Validate (from smoke)',
# verify you see both markers in logs:
#   RV_MARKER=call-operator-v2
#   RV_SCRIPT_VERSION=2025-08-28-v2
# Do not re-run old jobs; ensure a new run is created from the latest commit.

## Manual git commands (additional timestamp: 20250828_000501)

# Run in Git Bash at repo root (ASCII-safe path recommended, e.g., C:\\w\\BotG)
mkdir -p /c/w
cd /c/w
git clone https://github.com/baosang12/BotG.git
cd /c/w/BotG
ts=20250828_000501
branch="ci/recon-fix-${ts}"
git checkout -b "$branch"

# Copy updated files into this clone (overwrite):
#   .github/workflows/ci.yml
#   scripts/ci_reconstruct_validate.ps1

# If identity missing, set it first:
# git config user.email "you@example.com"
# git config user.name "Your Name"

git add .github/workflows/ci.yml scripts/ci_reconstruct_validate.ps1
git commit -m "CI: call-operator for recon validate + markers (fix runner quoting)"
git fetch origin
git merge origin/main
git push -u origin HEAD

# Check Actions runs for this branch and confirm markers in logs as above.
